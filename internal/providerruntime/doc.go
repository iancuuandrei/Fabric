// Package providerruntime executes one controller-owned structured inference
// call through a configured provider API. It is a model transport runtime, not
// a coding-agent runtime: it has no tool loop, shell, editor, session, OpenCode,
// proxy, or process dependency.
package providerruntime
