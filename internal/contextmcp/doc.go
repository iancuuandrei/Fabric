// Package contextmcp exposes a durable context broker through an owned loopback
// MCP listener. Invocation admission and host and candidate lifetimes remain
// the controller's responsibility.
package contextmcp
