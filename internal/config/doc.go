// Package config owns strict TOML configuration and its canonical run identity.
// It declares required checks but never executes them. See
// docs/specifications/repository-policy.md; runtime profiles are owned by runtime.
package config
