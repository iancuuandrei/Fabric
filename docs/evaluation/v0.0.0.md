# v0.0.0 experimental source checkpoint

This snapshot is published for code review. **M2 remains NO-GO.** It is not the
first trusted bootstrap checkpoint, a performance benchmark or production release.
The machine-readable companion is [v0.0.0.json](v0.0.0.json).

## Latest observed attempt

M2q used Luna Medium planning and Muse Spark 1.3 Contributor Free writing.
The plan was approved. Muse completed seven sequential context-tool calls and
one structured capture, all with matching provider receipts. The runtime result
settled and the process/proxy/context broker closed. No retry or fallback was
observed in those traces; that is not a guarantee about unobserved behavior.

The controller rejected the proposal before applying any changes: file preparation
sorts changes by path, but replay compared them with the original order. The
candidate stayed clean at its bootstrap base. Deterministic verification, fixer,
reviewer and candidate commit were **NOT RUN**. Code correctness is therefore unknown.

Writer usage was 311,250 input and 23,444 output tokens. Reasoning 14,460 is a
subset of output, and cached input 248,343 is a subset of input. These figures
are observed accounting, not cost estimates or performance conclusions.

## R33 local correction

Replay compares sorted copies of complete change records, retaining the original
model output and all content/metadata. Duplicate paths and substituted changes
still fail. Focused tests cover unsorted three-file proposals, content/path/mode
substitution, duplicate rejection and unchanged raw output. Independent review
was CLEAR for this bounded change.

`go test ./internal/control ./internal/fileeffects ./internal/cli -count=1`
passed locally in 423.609 seconds, followed by the runner build
and offline CLI configuration admission. This is not a full-repository test claim.
The source and binary identities are in the JSON companion. **No live attempt
has run after R33**, following the user's explicit pause for a model handoff.

## Runtime dependency

Qualification currently depends on the [R29/R31 OpenCode patches](../../third_party/opencode/qualification/README.md).
R29 preserves complete structured-output history readback. R31 admits `auto`
tool choice only for the exact Muse provider/model pair. The structured-capture
contract remains mandatory. Neither patch is automatically approved for production.

## Publication boundary

The public tree is a separate copy of R33 source plus public documentation,
sanitized qualification summaries and the exact MIT-licensed diagnostic patch.
One parser fixture replaces provider identifiers and encrypted reasoning with
synthetic values; matching assertions preserve the test contract. The provider
gateway package passed again on this public derivative (2.462 seconds).
Private reference identifiers are omitted. Credentials, raw model exchanges,
state databases, session directories, build artifacts and previous candidate
worktrees are excluded. Original local evidence and the canonical checkout were
not changed. License and donor notices are retained.

This initial source commit does not promote v0.0.1. A genuine fresh M2 end-to-end
PASS and a separate human promotion decision remain outstanding.
